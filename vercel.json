{
  "buildCommand": "npm install --foreground-scripts && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install --foreground-scripts",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
