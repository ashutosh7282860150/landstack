{
  "installCommand": "npm install --ignore-scripts",
  "buildCommand": "npm install --ignore-scripts && node node_modules/esbuild/install.js && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
